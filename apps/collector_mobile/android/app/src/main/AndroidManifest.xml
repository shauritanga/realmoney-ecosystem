<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Declared explicitly: only the debug manifest injected INTERNET, so a release
         build had no networking permission of its own. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Reads the device call log after a call so talk time is recorded automatically
         and the collector cannot mistype it. NOTE: Google Play restricts READ_CALL_LOG
         to eligible use cases (default dialer, caller ID, spam blocking); a collections
         CRM is not one, so this build is for internal distribution (Managed Google Play
         private app, or direct APK/MDM to company devices). If a public listing is ever
         required, drop this permission and switch DurationSource to IN_APP_TIMER, which
         needs no permission. -->
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>

    <!-- The call_log plugin's own manifest requests these; we only ever read. Leaving
         WRITE_CALL_LOG in place would claim an ability the app never uses and widen
         the Play Store restriction problem for no benefit, and READ_PHONE_STATE /
         READ_PHONE_NUMBERS expose the handset's own subscriber details, which this
         app has no reason to see. -->
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" tools:node="remove"/>

    <application
        android:label="RealMoney Collector"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Without these, package visibility on Android 11+ makes canLaunchUrl
             return false for tel:, sms: and https:, so Call / SMS / WhatsApp silently
             did nothing. Note the SMS scheme is `smsto`: url_launcher maps an sms:
             URI onto ACTION_SENDTO with that scheme, and declaring `sms` does not
             match. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="smsto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
